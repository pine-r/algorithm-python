二叉堆是实现优先队列的基础
最大堆的堆顶是整个堆中的最大元素
最小堆的堆顶是整个堆中的最小元素

优先队列不再遵循先入先出的原则，而是分两种情况：
1、最大优先队列，无论入队顺序如何，都是当前最大的元素优先出队
2、最小优先队列，无论入队顺序如何，都是当前最小的元素优先出队

入队操作：
1、插入新节点
2、新节点上浮至合适的位置
时间复杂度： O(logn)

出队操作：
1、将原堆顶节点出队
2、把最后的节点替换到堆顶位置
3、让堆顶节点再下沉
时间复杂度： O(logn)